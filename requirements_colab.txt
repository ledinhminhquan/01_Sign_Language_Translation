# Colab/H100 training deps — installed by the notebook BEFORE `pip install -e . --no-deps`.
# torch is preinstalled on Colab; we add the ML / pose / serving / report stack. The notebook then
# installs the package itself with --no-deps so it does not perturb Colab's resolved torch/CUDA.
transformers>=4.40
datasets>=2.18
accelerate>=0.30
sacrebleu>=2.4
mediapipe>=0.10
opencv-python>=4.8
fastapi>=0.110
uvicorn[standard]>=0.29
gradio>=4.0
reportlab>=4.0
python-pptx>=0.6.23
matplotlib>=3.7
numpy>=1.23
pyyaml>=6.0
pydantic>=2.0
